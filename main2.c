// C program for generic linked list
#include<stdio.h>
#include<stdlib.h>


/* Driver program to test above function */
int main()
{
	int t;
	while(scanf("%d", &t) != 0){
		printf("%d", t);
	};

	fseek(stdin, 0, SEEK_END);
	num = ftell(stdin);
	

	return 0;
}
